package org.xandercat.ofe;

/**
 * Indicator for if a matched object has been added, changed, or removed 
 * as compared to the prior list of matched objects.
 * 
 * @author Scott Arnold
 */
public enum ChangeType {
	ADDED, CHANGED, REMOVED
}
